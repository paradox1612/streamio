apiVersion: v2
name: streambridge
description: Helm chart for the StreamBridge frontend, backend, and optional PostgreSQL
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: redis
    version: "19.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
